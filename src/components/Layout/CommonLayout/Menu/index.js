import Style from "./Menu.module.scss";

const Menu = ({ props }) => {

  return (
    <header className={`${Style.header} fixed-top`}>
     {/* header */}
    </header>
  );
};

export default Menu;
